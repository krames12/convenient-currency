<!DOCTYPE html>
<html lang="en">
<head>
  <script src="https://use.fontawesome.com/b28b7d5db1.js"></script>
  <script src="https://code.jquery.com/jquery-3.1.1.min.js"
  integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8="
  crossorigin="anonymous"></script>
  <link href="public/css/reset.css" rel="stylesheet" />
   <link href="public/css/style.css" rel="stylesheet" />
   <meta http-equiv="content-type" content="text/html; charset=UTF-8">
   <meta name="viewport" content="initial-scale=1.0">
  <title>Convenient Currency</title>
</head>
<body>
<div class="site-wrapper">
    <h1>Currency Conversion</h1>
    <div class="change-container">
      <input type="number" id="currency-input" class="num-input" placeholder="0.00" min='0' />
      <select id="converting-from" class="currency-select">
        <% for(currency of info) { %>
          <% if(currency == "USD") { %>
            <option value=<%= currency %> selected>
              <%= currency %>
            </option>
          <% } else { %>
            <option value=<%= currency %>>
              <%= currency %>
            </option>
          <% } %>
        <% } %>
      </select>
    </div>
    
    <div class="change-container">
      <input type="text" id="dollar-amount" class="num-input" disabled />
      <select id="converting-to" class="currency-select">
        <% for(currency of info) { %>
          <% if(currency == "GBP") { %>
            <option value=<%= currency %> selected>
              <%= currency %>
            </option>
          <% } else { %>
            <option value=<%= currency %>>
              <%= currency %>
            </option>
          <% } %>
        <% } %>
      </select>
    </div>
    <footer>
      <p>Exchange data from the European Central Bank via <a href="http://fixer.io/">Fixer.io</a></p>
      <p>Project on <a href="https://github.com/krames12/convenient-currency">Github</a></p>
    </footer>
</div>
<script src="public/index.js"></script>
</body>
</html>